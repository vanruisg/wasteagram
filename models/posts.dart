class Post {
  DateTime date;
  String imageURL;
  double longitude;
  double latitude;
  int quantity;

  Post(
      {this.date, this.imageURL, this.longitude, this.latitude, this.quantity});
}
